<div id="projects">
  <p><label for="sample_set_project_id">Project</label><br/>
  <select name="sample_set[project_id]" class="required">
    <option value="">Please Select</option>
    <%= options_from_collection_for_select @projects, :id, :name, @project && @project.id %>
  </select>
  (<%= link_to_remote 'Add a new Project', :update => "projects",
       :url => { :controller => 'projects', :action => 'new_inline' } %>)</p>
</div>
